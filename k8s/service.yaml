apiVersion: v1
kind: Service

metadata:
  name: airflow-service
  namespace: airflow

spec:
  selector:
    app: airflow-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 31001

  type: NodePort